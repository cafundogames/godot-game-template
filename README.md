# Godot Game Template

Make sure to configure `project.config.json` for version publishing.

Make sure to configure `secrets.BUTLER_CREDENTIALS` in repo secrets for itch.io publishing.

Make sure all enviroment variables and matrices in `.github/worflows/release.yml` are properly setup.

Make sure all presets in `.github/worflows/ci.yml` are properly setup.

Make sure all preset packages in `flake.nix` are properly setup.
